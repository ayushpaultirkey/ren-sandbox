const HANDLERS = {
    WEB: "WEB"
};

const FS_HANDLERS = {
    WEB: {
        path: "fs",
        file: "web"
    },
};

const ARGV_HANDLERS = {
    WEB: {
        path: "argv",
        file: "web"
    }
}

export {
    FS_HANDLERS,
    ARGV_HANDLERS,
    HANDLERS
};